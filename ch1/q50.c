/* Program to create enumerate datatype of for 12 months */
#include <stdio.h>
int main() {
	enum month{jan, feb, mar, apr, may, jun, jul, aug, sep, oct, nov, dec};
	printf("Jan=%d\n", jan);
	printf("Feb=%d\n", feb);
	return 0;
}